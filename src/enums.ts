// Enums
//////////////////////////////////////////

export enum LanguageDirection{
    ltr = 0,
    rtl = 1
}

export enum TranslationStatusResult {
    ignored = 1,
    succeeded = 2,
    failed = 3
}

export enum TranslatorProvider {
    none = 0,
    Azure = 1
}

export enum UrlLanguageLocation {
    none = 0,
    subdirectory = 1
}